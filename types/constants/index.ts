export type NavLink = {
  label: string;
  href: string;
};

export type SocialLink = {
  icon: React.ReactNode;
  href: string;
  label: string;
};

export type ContactInfo = {
  icon: React.ReactNode;
  text: string;
  href: string;
};
